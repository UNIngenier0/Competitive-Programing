#include <bits/stdc++.h>
#include <math.h>
using namespace std;
#define f(i,a,b) for (int i =a; i<b; i++)
int main (){
    int n;
    while(cin>>n){
        if(!n) break;
        int a,b,c;
        cout<<b<<endl;
    }
    return 0;
}