#include <bits/stdc++.h>
using namespace std;
int main() {
    // Esto debe estar en el libro de cses
    // Fuerza bruta es probar todos los casos para encontrar una solución
    // 10^8 es lo que un pc puede hacer por segundo
    // un doble o triple for nos puede incrementar la complejidad
    // amo este tema está chimbisima
    // hashmap hash(n)

    /*vector<int> u, subset;
    void search(int k){
        if(k==n){
            for(auto x; subset){
                cout<<x<<||;
            }
            cout<<endl;
        }
    }*/

    

}