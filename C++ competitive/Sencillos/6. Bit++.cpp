#include <bits/stdc++.h>
using namespace std;
#define f(i,a,b) for(int i=a;i<b;i++)

int main(){
    int lines;
    string a,mas= "+", menos="-";
    f(i,0,lines){
        cin>>a;
        if( ){

        }
    }
    return 0;
}